from django.apps import AppConfig


class AgrobusinessConfig(AppConfig):
    """Class to configure some options on the app"""

    default_auto_field = "django.db.models.BigAutoField"
    name = "agrobusiness"
